import React from 'react';
import SharedMER4Form from '../../../Components/MER4/SharedMER4Form';

/**
 * MER4 Form 3 Index - Institutional Student Programs and Services (Annexes H-O)
 * Uses SharedMER4Form component with formNumber=3
 */
const Form3Index = (props) => {
  return <SharedMER4Form formNumber={3} {...props} />;
};

export default Form3Index;
