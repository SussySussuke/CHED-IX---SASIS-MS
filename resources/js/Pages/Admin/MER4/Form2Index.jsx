import React from 'react';
import SharedMER4Form from '../../../Components/MER4/SharedMER4Form';

/**
 * MER4 Form 2 Index - Student Welfare Services (Annexes E-G)
 * Uses SharedMER4Form component with formNumber=2
 */
const Form2Index = (props) => {
  return <SharedMER4Form formNumber={2} {...props} />;
};

export default Form2Index;
