import React from 'react';
import SharedMER4Form from '../../../Components/MER4/SharedMER4Form';

/**
 * MER4 Form 1 Index - Student Welfare Services (Annexes A-D)
 * Uses SharedMER4Form component with formNumber=1
 */
const Form1Index = (props) => {
  return <SharedMER4Form formNumber={1} {...props} />;
};

export default Form1Index;
