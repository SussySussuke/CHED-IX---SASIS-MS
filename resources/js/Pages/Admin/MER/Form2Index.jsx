import React from 'react';
import SharedMERForm from '../../../Components/MER/SharedMERForm';

/**
 * Form 2 Index - Student Welfare Services (Annexes E-G)
 * Uses SharedMERForm component with formNumber=2
 */
const Form2Index = (props) => {
  return <SharedMERForm formNumber={2} {...props} />;
};

export default Form2Index;
